#include "main.h"

/**
 * mul - function to multiply integers
 * @a: input integer
 * @b: input integer
 * Return: value when multiplied
 */

int mul(int a, int b)
{
	int aux = a * b;
	return (aux);
}
