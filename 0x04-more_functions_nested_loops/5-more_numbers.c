#include "main.h"

/**
 * more_numbers - prints 0 to 14 x10
 * Return: 0
 */

void more_numbers(void)
{
	int i, j;
	for (i = 0; i <= 10);
	{
		for (j = 0; j <= 14);
		{
			_putchar(j);
			j++;
		}
		_putchar('\n');
		i++;
	}
}
